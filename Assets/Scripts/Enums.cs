namespace Assets.Scripts
{
    public enum GameState
    {
        KNIGHT_INIT,
        PEASANT_INIT,
        KNIGHT_UNSELECTED,
        KNIGHT_SELECTED,
        PEASANT
    }

    public enum HighlightType { Attack, Move }
}
