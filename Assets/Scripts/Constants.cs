using System;
using UnityEngine;

namespace Assets.Scripts
{
    class Constants
    {
        public static readonly Color AttackColor = ColorConverter.GetUnityColor(255f, 0f, 0f);
        public static readonly Color MoveColor = ColorConverter.GetUnityColor(102f, 125f, 210f);
    }
}
